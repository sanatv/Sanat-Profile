name: Keep profile warm

# Pings the deployed sanat-profile so the Render free-tier instance is touched
# on a schedule. A 20-min cadence keeps it awake most of the time while staying
# well under the 750 free instance-hours/month cap (~540 hrs/month at 24/7).
# NOTE: Render sleeps after 15 min idle, so there is still a brief nap each
# cycle — change the cron to '*/10 * * * *' if you want it fully warm.
#
# Set the live URL once as a repo variable (Settings → Secrets and variables →
# Actions → Variables → New variable):  PROFILE_URL = https://your-app.onrender.com
# If unset, it falls back to the conventional Render URL below.

on:
  schedule:
    - cron: "*/20 * * * *" # every 20 min (fires at :00, :20, :40 UTC)
  workflow_dispatch: {} # allows manual "Run workflow" from the Actions tab

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping /api/health
        run: |
          BASE="${{ vars.PROFILE_URL || 'https://sanat-profile.onrender.com' }}"
          URL="${BASE%/}/api/health"
          echo "Pinging $URL"
          for attempt in 1 2 3 4; do
            code=$(curl -s -o /dev/null -w "%{http_code}" --max-time 75 "$URL" || echo "000")
            if [ "$code" = "200" ]; then
              echo "OK ($code) on attempt $attempt"
              exit 0
            fi
            echo "Attempt $attempt got HTTP $code — instance may be waking; retrying in 30s"
            sleep 30
          done
          echo "Ping failed after retries"
          exit 1
